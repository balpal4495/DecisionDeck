<!-- Chronicle Summary v1 — temporal orientation for agents. Use for sequence context; query Oracle by entry ID for full reasoning. -->

## Week 2026-W20

### (no work context — query Oracle by entry ID for details)
- **[3b974b13]** scripts/sync-jira.ts, lib/jira/mappers.ts, lib/env.ts, tests/sync/jira.mappers.test.ts, package.json, db/schema.ts, .env.local — `validated` (0.95) — Jira integration uses the Jira REST API (read-only) via POST /rest/api/3/search/jql, runs as a standalone Node.js script, and uses the Jira issue key (e.g. PROJ-123) as the external ID for idempotent upsert into the WorkItems table.. Jira integration uses the Jira REST API (read-only) via POST /rest/api/3/search/jql, runs as a standalone Node.js script, and uses the Jira issue key (e.g. PROJ-123) as the external ID for idempotent upsert into the WorkItems table.
- **[7eb50c45]** components/DeliveryGL.tsx, components/DeliveryGL.module.css, app/timeline/page.tsx, app/timeline/page.module.css, lib/delivery-tree.ts — `open` (0.80) — Two visual encodings on a single circle (inner fill = node type, outer ring = status) make Jira/GitHub provenance and workflow state legible at a glance without ambiguity.. Replace accordion drop-down delivery view with a WebGL node graph rendered via deck.gl (OrthographicView + ScatterplotLayer + PathLayer + TextLayer), surfacing Epic→Sprint→Story→Sub-task→PR hierarchy with type-fill colours and status-ring colours per node.
- **[ef3843f7]** scripts/sync-jira.ts, lib/graph.ts — `open` (0.97) — Jira instance-specific type names silently break whitelist JQL filters — this instance uses 'Sub-task' (hyphenated) not 'Subtask' (one word). Exclusion patterns are future-proof; enumeration is fragile. A post-sync backfill pass is also required to catch Done tickets and edge-case types still referenced by open PRs.. Jira sync must use `issuetype NOT IN (Epic)` instead of an explicit whitelist, and include a PR-key backfill pass for tickets absent from the main sweep.
- **[de78f9f4]** lib/triage.ts, app/triage/page.tsx, tests/triage/triage.test.ts, app/layout.tsx — `open` (0.90) — A heuristic-only triage classifier (lib/triage.ts) classifies all synced work items into active/queued/blocked/stale/done/abandoned using source-system dates from rawData. Council constraint: classification reason always shown, no person-level grouping, external URL always available.
- **[d99d4df1]** scripts/sentinel-pr.ts, quorum/modules/sentinel/ — `open` (0.95) — Sentinel is a standing codebase-wide Chronicle coverage map; all modules always appear; risk is derived from coverage % only, never from what changed in the current PR.. Sentinel is a standing codebase-wide Chronicle coverage map; all modules always appear; risk is derived from coverage % only, never from what changed in the current PR.
- **[d306916f]** scripts/sentinel-pr.ts, quorum/modules/sentinel/coverage.ts, app/, components/, .github/ — `open` (0.95) — Sentinel coverage scanner scans .ts, .tsx, and .yml files; .tsx covers Next.js pages and components, .yml covers GitHub workflow files.. Sentinel coverage scanner scans .ts, .tsx, and .yml files; .tsx covers Next.js pages and components, .yml covers GitHub workflow files.
- **[8bbabcec]** scripts/sync-jira.ts, lib/jira/mappers.ts, tests/sync/jira.mappers.test.ts, lib/env.ts, package.json — `open` (0.95) — Jira epics and tickets are synced read-only via POST /rest/api/3/search/jql into the WorkItems table using idempotent upsert.. Jira epics and tickets are synced read-only via POST /rest/api/3/search/jql into the WorkItems table using idempotent upsert.
- **[f6ab71da]** phases.md, app/decisions/, scripts/sync-github.ts, scripts/sync-jira.ts, scripts/sync-confluence.ts — `open` (0.95) — Data integrations (GitHub, Jira, Confluence) must be built before the Decision Ledger so decisions link to real operational signals from day one.. Data integrations (GitHub, Jira, Confluence) must be built before the Decision Ledger so decisions link to real operational signals from day one.
- **[95327486]** scripts/sync-confluence.ts, db/schema.ts, .env.local — `open` (0.85) — Confluence integration indexes page titles, labels, and URLs only; full page content is not stored locally to avoid large storage payloads and PII exposure.. Confluence integration indexes page titles, labels, and URLs only; full page content is not stored locally to avoid large storage payloads and PII exposure.
- **[901812db]** db/schema.ts, db/migrations/, lib/db.ts, package.json — `open` (0.95) — SQLite with Drizzle ORM is the sole data store; no cloud database or SaaS backend is required to run DecisionDeck.. SQLite with Drizzle ORM is the sole data store; no cloud database or SaaS backend is required to run DecisionDeck.
- **[88f9a0f2]** scripts/sync-jira.ts, db/schema.ts, .env.local — `open` (0.90) — Jira integration uses the Jira REST API (read-only), runs as a standalone Node.js script, and uses the Jira issue key (e.g. PROJ-123) as the external ID for idempotent upsert.. Jira integration uses the Jira REST API (read-only), runs as a standalone Node.js script, and uses the Jira issue key (e.g. PROJ-123) as the external ID for idempotent upsert.
- **[82baf04a]** scripts/sync-github.ts, package.json, .env.local — `open` (0.95) — GitHub data is pulled read-only via `gh api` CLI, supports GitHub Enterprise via GH_HOST, and runs as a standalone Node.js script — not inside the Next.js server.. GitHub data is pulled read-only via `gh api` CLI, supports GitHub Enterprise via GH_HOST, and runs as a standalone Node.js script — not inside the Next.js server.
- **[5de274f2]** scripts/, package.json, lib/db.ts — `open` (0.95) — All data sync commands (GitHub, Jira, Confluence) run as standalone Node.js scripts invoked via `npm run sync:*`; they write to SQLite directly and do not run inside the Next.js server process.. All data sync commands (GitHub, Jira, Confluence) run as standalone Node.js scripts invoked via `npm run sync:*`; they write to SQLite directly and do not run inside the Next.js server process.
- **[4f9001e4]** app/, lib/report-generator.ts, db/schema.ts, scripts/ — `open` (1.00) — Individual engineer metrics (commit counts, PR counts, review speed per person, lines of code) are permanently excluded from DecisionDeck with no exceptions without an explicit superseding decision.. Individual engineer metrics (commit counts, PR counts, review speed per person, lines of code) are permanently excluded from DecisionDeck with no exceptions without an explicit superseding decision.
- **[305c534b]** app/, components/, next.config.ts — `open` (0.90) — CSS Modules are the only styling mechanism in this project; Tailwind, PostCSS configuration, and any utility-class framework are permanently excluded.. CSS Modules are the only styling mechanism in this project; Tailwind, PostCSS configuration, and any utility-class framework are permanently excluded.
